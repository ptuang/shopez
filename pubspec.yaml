name: projecttwo
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  firebase_core: ^2.24.0
  flutter:
    sdk: flutter
  //these are dependencies for productscreen.dart
  carousel_slider: ^4.2.1
  cloud_firestore: ^4.13.3
  firebase_auth: ^4.15.0
  flutter_rating_bar: ^4.0.1
  flutter_screenutil: ^5.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
